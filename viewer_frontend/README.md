# EPS Viewer Frontend

Install dependencies:

```bash
npm install
```

Run the dev server:

```bash
npm run dev
```

If the API is not running on `http://localhost:8000`, set:

```bash
VITE_API_BASE_URL=http://localhost:8000
```
