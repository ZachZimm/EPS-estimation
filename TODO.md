# TODO

- Try conformal calibration on top of the current trimmed global model or quantile runs. The current quantile intervals are under-covering badly, so post-hoc calibration could improve interval reliability without changing the core model.
